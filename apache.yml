
---
  - hosts : webserver
    become : True
    become_method : sudo
    become_user : root
    gather_facts : True

    tasks:

    - name: Install a list of packages
      apt :
       name : "{{packages}}"
       state : present
      vars:
       packages :
        - net-tools
        - httpd
       
    - name : Start service httpd , if not started
      service :
       name : httpd started
       state : started
       enabled : True
       
    - name : Deploy Statsic website
      copy :
       src : index.html
       dest : /var/www/html/


